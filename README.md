# File Downloader
```
Usage:
  -chunks int
        number of chunks in which the file is to be downloaded (default 10)
  -name string
        name to save the file with
  -path string
        path in which the downloaded file is to be saved (default ".")
  -url string
        URL to download from
```